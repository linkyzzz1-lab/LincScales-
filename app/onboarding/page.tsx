/* eslint-disable @next/next/no-html-link-for-pages */
"use client";
import { FormEvent, useState } from "react";
export default function OnboardingPage() { const [result, setResult] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const response = await fetch("/api/onboarding", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: form.get("name"), email: form.get("email"), company: form.get("company"), orderReference: form.get("orderReference"), goals: form.get("goals"), website: form.get("website") }) }); const data = await response.json(); setResult(response.ok ? "Onboarding details saved. We’ll use them to start your project." : data.error ?? "Something went wrong."); } return <main className="min-h-screen"><nav className="mx-auto flex max-w-6xl items-center justify-between px-6 py-6"><a href="/" className="text-sm font-bold tracking-[.18em] text-white">← LINCSCALES</a><span className="text-xs text-slate-500">Customer onboarding</span></nav><div className="mx-auto max-w-6xl px-6 py-14"><p className="eyebrow mb-5">Welcome to the workspace</p><h1 className="max-w-2xl text-5xl font-semibold tracking-[-.06em] text-white">Let’s get your project moving.</h1><p className="mt-5 max-w-xl leading-7 text-slate-400">Use the reference from your service request to securely connect your brief to the right project.</p><form onSubmit={submit} className="card mt-12 grid max-w-3xl gap-5 p-7 sm:grid-cols-2"><label className="text-sm text-slate-300">Your name<input required name="name" className="mt-2 w-full border border-white/15 bg-white/5 px-4 py-3 text-white" /></label><label className="text-sm text-slate-300">Work email<input required type="email" name="email" className="mt-2 w-full border border-white/15 bg-white/5 px-4 py-3 text-white" /></label><label className="text-sm text-slate-300">Company<input name="company" className="mt-2 w-full border border-white/15 bg-white/5 px-4 py-3 text-white" /></label><label className="text-sm text-slate-300">Order reference<input required name="orderReference" placeholder="LS-20260923-ABC123" className="mt-2 w-full border border-white/15 bg-white/5 px-4 py-3 font-mono text-white" /></label><label className="text-sm text-slate-300 sm:col-span-2">What should we know before kickoff?<textarea required name="goals" rows={5} className="mt-2 w-full border border-white/15 bg-white/5 px-4 py-3 text-white" /></label><label className="hidden">Website<input name="website" tabIndex={-1} autoComplete="off" /></label><button className="button-blue px-5 py-3 text-sm font-bold sm:w-fit">Save onboarding details ↗</button>{result && <p aria-live="polite" className="text-sm text-[#74b7ff] sm:col-span-2">{result}</p>}</form></div></main>; }