"use client";
import { FormEvent, useState } from "react";
const services = ["Meta Ads setup", "Ongoing campaign management", "Creative testing", "AI chatbot"];
export default function PlanBuilderPage() { const [result, setResult] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const response = await fetch("/api/plan-requests", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: form.get("name"), email: form.get("email"), company: form.get("company"), businessType: form.get("businessType"), goals: form.get("goals"), budget: form.get("budget"), desiredServices: form.getAll("services"), website: form.get("website") }) }); const data = await response.json(); setResult(response.ok ? `Plan request received. Your reference is ${data.reference}.` : data.error ?? "Something went wrong."); } return <main className="min-h-screen"><nav className="mx-auto flex max-w-5xl items-center justify-between px-6 py-6"><a href="/packages" className="text-sm font-bold tracking-[.18em] text-white">← PACKAGES</a><span className="eyebrow">Custom plan builder</span></nav><div className="mx-auto max-w-5xl px-6 py-12"><p className="eyebrow mb-5">Step 1 / brief</p><h1 className="text-5xl font-semibold tracking-[-.06em] text-white">Shape the right starting point.</h1><p className="mt-5 max-w-xl leading-7 text-slate-400">Tell us what you’re building and we’ll turn your inputs into a preliminary scope for review.</p><form onSubmit={submit} className="card mt-12 grid max-w-4xl gap-5 p-7 sm:grid-cols-2"><label className="text-sm text-slate-300">Your name<input required name="name" className="mt-2 w-full border border-white/15 bg-white/5 px-4 py-3 text-white" /></label><label className="text-sm text-slate-300">Work email<input required type="email" name="email" className="mt-2 w-full border border-white/15 bg-white/5 px-4 py-3 text-white" /></label><label className="text-sm text-slate-300">Company<input name="company" className="mt-2 w-full border border-white/15 bg-white/5 px-4 py-3 text-white" /></label><label className="text-sm text-slate-300">Business type<select required name="businessType" className="mt-2 w-full border border-white/15 bg-[#0d1c2d] px-4 py-3 text-white"><option value="">Select one</option><option>Professional services</option><option>E-commerce</option><option>Local business</option><option>SaaS / technology</option><option>Creator / personal brand</option><option>Other</option></select></label><label className="text-sm text-slate-300">Approximate monthly budget<select required name="budget" className="mt-2 w-full border border-white/15 bg-[#0d1c2d] px-4 py-3 text-white"><option value="">Select one</option><option>Under $1,000</option><option>$1,000 - $5,000</option><option>$5,000 - $10,000</option><option>$10,000+</option><option>Not sure yet</option></select></label><fieldset className="text-sm text-slate-300"><legend>Desired services</legend><div className="mt-3 grid gap-3">{services.map((service) => <label key={service} className="flex items-center gap-3"><input type="checkbox" name="services" value={service} className="accent-[#2F8DF5]" />{service}</label>)}</div></fieldset><label className="text-sm text-slate-300 sm:col-span-2">Primary goal<textarea required name="goals" rows={5} className="mt-2 w-full border border-white/15 bg-white/5 px-4 py-3 text-white" placeholder="What would success look like?" /></label><label className="hidden">Website<input name="website" tabIndex={-1} autoComplete="off" /></label><button className="button-blue px-5 py-3 text-sm font-bold sm:col-span-2 sm:w-fit">Get my preliminary plan ↗</button>{result && <p aria-live="polite" className="text-sm text-[#74b7ff] sm:col-span-2">{result}</p>}</form></div></main>; }