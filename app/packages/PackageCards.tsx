"use client";
type Package = { slug: string; name: string; category: string; priceCents: number; frequency: string; description: string; features: string[] };
export default function PackageCards({ packages }: { packages: Package[] }) { const formatPrice = (cents: number) => new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 }).format(cents / 100); return <div className="mt-14 grid gap-10 lg:grid-cols-2">{["META_ADS", "AI_CHATBOT"].map((category) => <section key={category}><div className="mb-5 flex items-end justify-between"><div><p className="eyebrow">{category === "META_ADS" ? "Meta Ads" : "AI chatbot marketplace"}</p><h2 className="mt-2 text-2xl font-semibold text-white">{category === "META_ADS" ? "Campaign momentum" : "Always-on assistance"}</h2></div><span className="text-xs text-slate-500">No payment required</span></div><div className="space-y-3">{packages.filter((item) => item.category === category).map((item) => <article key={item.slug} className="card card-hover p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><h3 className="text-xl font-semibold text-white">{item.name}</h3><p className="mt-2 max-w-md text-sm leading-6 text-slate-400">{item.description}</p></div><div className="text-right"><span className="text-3xl font-semibold tracking-[-.05em] text-white">{formatPrice(item.priceCents)}</span><span className="block text-xs text-slate-500">{item.frequency === "ONE_TIME" ? "one-time" : "per month"}</span></div></div><div className="mt-5 flex flex-wrap gap-2">{item.features.map((feature) => <span key={feature} className="border border-white/10 px-2 py-1 text-xs text-slate-400">{feature}</span>)}</div><a href={`/checkout?items=${item.slug}`} className="button-blue mt-6 inline-block px-5 py-3 text-sm font-bold">Review {item.name} ↗</a></article>)}</div></section>)}</div>; }
