/* eslint-disable @next/next/no-html-link-for-pages, @next/next/no-location-assign-relative-destination */
"use client";
import { FormEvent, useState } from "react";
export default function AdminLoginPage() { const [error, setError] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const response = await fetch("/api/admin/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ email: form.get("email"), password: form.get("password") }) }); if (response.ok) window.location.href = "/admin"; else setError((await response.json()).error ?? "Unable to sign in."); } return <main className="grid min-h-screen place-items-center px-6"><div className="w-full max-w-md"><a href="/" className="text-sm font-bold tracking-[.18em] text-white">← LINCSCALES</a><div className="card mt-8 p-8"><p className="eyebrow mb-4">Private operations</p><h1 className="text-3xl font-semibold tracking-[-.04em] text-white">Admin sign in</h1><p className="mt-4 text-sm leading-6 text-slate-400">Use the administrator credentials configured in the server environment. No default account exists.</p><form onSubmit={submit} className="mt-8 grid gap-5"><label className="text-sm text-slate-300">Email<input required type="email" name="email" autoComplete="username" className="mt-2 w-full border border-white/15 bg-white/5 px-4 py-3 text-white" /></label><label className="text-sm text-slate-300">Password<input required type="password" name="password" autoComplete="current-password" className="mt-2 w-full border border-white/15 bg-white/5 px-4 py-3 text-white" /></label><button className="button-blue px-5 py-3 text-sm font-bold">Sign in ↗</button>{error && <p role="alert" className="text-sm text-red-300">{error}</p>}</form></div></div></main>; }